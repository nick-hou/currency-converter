import React from 'react';

function Footer() {
  return(
    <div>
      <h4>Made by Nick Hou. Built with <a href="https://fixer.io/">fixer.io</a></h4>
      <p>Note: app currently only works over HTTP. If getting an error, please reload the page in HTTP instead of HTTPS.</p>
    </div>
  )
}

export default Footer;
